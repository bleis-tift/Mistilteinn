2  TODO  前回の非git-nowコミットからのリビジョングラフの表示
         選択してresetなども可能。
         resetする前に、tagが付いていなければtagを付ける。
         fixupボタンを押したときに派生ブランチのタグは削除。
6  TODO  ビルド後に自動でテストを実行
7  TODO  private buildの実行
8  TODO  pull
9  TODO  private buildの結果の通知
10  TODO  各機能の個別のon/off
11  TODO  設定画面と設定ファイル
12  BUG  プロジェクトの削除や、ファイルの削除でgit-nowが走らない
         それを拾えそうなイベントが拾ってくれない。
13  BUG  プロジェクトの追加で何もせずに放っておくとgit-nowが走らない
         プロジェクトを追加したらそのプロジェクトのファイルをいじるだろうから、
         そちらでgit-nowが走るのでそこまで問題があるわけではない。
14  TODO  ticket listで切り替えるときに未コミット作業が残っている場合、
          * 一時保存コミットを作っておき、戻ってきたときに自動的にreset
          * 変更を切り替え後のブランチに持っていく
          * 切り替えのキャンセル
          を選択できるようにする。

